<launch>

  <arg name="db" default="false" />
  <arg name="db_path" default="$(find moveit_config_m1013)/default_warehouse_mongo_db" />
  <arg name="gripper" default="none"/>
  <arg name="color" default="white"/>
  <arg name="ns" default="dsr"/>
  <arg name="fake_execution" default="true"/>
  <arg name="debug" default="false" />
  <arg name="use_gui" default="false" />


  <!-- If needed, broadcast static tf for robot root -->
  <node pkg="tf" type="static_transform_publisher" name="amr2dsr"             args="-0.23 0 0.68 3.1416 0 0 R_001/base_link base_0 10" />
  <node pkg="tf" type="static_transform_publisher" name="footprint2base_link" args="0 0 0 0 0 0 R_001/base_footprint /R_001/base_link 10" />
  <node pkg="tf" type="static_transform_publisher" name="link62camera_link"   args="-0.118 0.020 -0.032 0 -1.572 0 link6 camera_link 10" />
  <!--<node pkg="tf" type="static_transform_publisher" name="link62camera_link"   args="0.118 0.0 0.032 0 -1.572 3.1416 link6 camera_link 10" />
  <node pkg="tf" type="static_transform_publisher" name="camera_calib_ar"     args="0 0.020 0 0 0 0 camera_link camera_calib_ar 10" />-->

  
  <!-- Load the URDF, SRDF and other .yaml configuration files on the param server -->
  <include file="$(find moveit_config_m1013)/launch/planning_context.launch">
    <arg name="load_robot_description" value="true"/>
  </include>


  <!-- We do not have a robot connected, so publish fake joint states -->
  <node name="joint_state_publisher" pkg="joint_state_publisher" type="joint_state_publisher" if="$(arg fake_execution)">
    <param name="use_gui" value="$(arg use_gui)"/>
    <rosparam param="source_list">[move_group/fake_controller_joint_states]</rosparam>
  </node>


  <!-- Given the published joint states, publish tf for the robot links -->
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" respawn="true" output="screen" />

  <!-- Run the main MoveIt! executable without trajectory execution (we do not have controllers configured by default) -->
  <include file="$(find moveit_config_m1013)/launch/move_group.launch">
    <arg name="allow_trajectory_execution" value="true"/>
    <arg name="fake_execution" value="$(arg fake_execution)"/>
    <arg name="info" value="true"/>
    <arg name="debug" value="$(arg debug)"/>
  </include>

  <!-- Run Rviz and load the default config to see the state of the move_group node -->
  <!--include file="$(find moveit_config_m1013)/launch/moveit_rviz.launch">
    <arg name="config" value="true"/>
    <arg name="debug" value="$(arg debug)"/>
  </include-->

  <!-- If database loading was enabled, start mongodb as well -->
  <include file="$(find moveit_config_m1013)/launch/default_warehouse_db.launch" if="$(arg db)">
    <arg name="moveit_warehouse_database_path" value="$(arg db_path)"/>
  </include>



</launch>
